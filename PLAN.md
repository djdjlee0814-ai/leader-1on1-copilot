# 개발 계획 (PLAN)

Leader 1:1 Copilot을 어떤 순서로 만들었고, 다음에 무엇을 할지 정리한 문서입니다.
기획 배경과 요구사항은 [PRD.md](PRD.md)를 참고하세요.

## 개발 원칙

1. **입력은 줄이고 결과물의 질을 올린다.** 입력 항목이 늘어나는 방향의 변경은 기본적으로 거절한다.
2. **AI는 사람을 규정하지 않는다.** 사실과 가설을 분리하고, 평가등급은 리더가 결정한다.
3. **단계마다 실제로 동작하는 상태를 유지한다.** 큰 구조 변경도 화면이 깨지지 않는 순서로 쪼갠다.
4. **기존 기능을 갈아엎지 않는다.** 재설계할 때도 동작 중인 로직과 저장 데이터는 보존한다.

## 진행 단계

### 1단계 — 최소 기능 (완료)

- Next.js 프로젝트 구성
- 팀원 정보 입력 → GROW 모델 기반 질문 생성
- AI 호출은 서버에서만 수행하도록 API Route 분리

### 2단계 — 코파일럿으로 전환 (완료)

"질문을 나열하는 도구"에서 "면담 진행을 돕는 도구"로 방향 전환.

- 면담 전 브리핑 / 면담 중 실시간 도움 / 면담 후 Action 정리로 구조 재편
- 대화 상태(Conversation State) 도입 — 확인된 사실과 가설을 분리해 관리
- 직원 발언이 들어오면 대화 초점과 다음 질문을 다시 계산
- 답변 유형별 후속 질문, 피해야 할 반응 제공

### 3단계 — 면담 유형 분리 (완료)

- 성과면담 / 성장·육성면담을 독립적인 두 유형으로 분리
- 성과 자료 자유 붙여넣기 입력 방식 추가
- 성장·육성면담의 주제 선택(복수 선택, "아직 잘 모르겠음" 포함)
- 면담 종료 후 평가등급(S~D) 기록 — AI는 판단 근거만 제시

### 4단계 — 입력 최소화와 결과물 재설계 (완료)

- 필수 입력을 4개로 축소, 나머지는 "조금 더 알려주기"로 이동
- 결과물을 시간 배분이 있는 면담 진행표(Conversation Map)로 재구성
- 단계별 핵심 질문 1개 + Leader Tip 구조
- 지난 면담 이어받기를 팀원 선택 기준으로 변경

### 5단계 — 배포 준비 (완료)

- Supabase Google/이메일 로그인, 초대 전용 가입(Before User Created 훅)
- 면담 기록을 브라우저 저장소에서 Postgres로 이전, 행 단위 보안(RLS) 적용
- 리더/관리자 권한 분리
- AI 호출 API에 로그인 검사 추가
- GitHub 연동, Vercel 자동 배포

### 6단계 — 화면 재설계 (완료)

- 검은 블록 선택 상태 제거, 절제된 강조색 하나로 통일
- 중첩 카드 제거, 헤어라인과 여백으로 정보 구분
- 면담 흐름을 시간 레일 타임라인으로 변경, 질문을 화면에서 가장 큰 요소로

## 남은 작업

| 우선순위 | 항목 | 비고 |
| --- | --- | --- |
| 높음 | Google 로그인 연결 마무리 | Google Cloud OAuth 클라이언트 발급 후 Supabase provider 활성화 필요 |
| 중간 | 관리자 화면 | 현재 초대 관리는 DB에서 직접 수행 |
| 중간 | 육성 계획 편집 | 현재 실시간 도움 모드를 거쳐야 생성됨 |
| 낮음 | 면담 녹음 및 회의록 생성 | 개인정보 동의 절차 설계가 선행되어야 함 |
| 낮음 | 팀 단위 공유 | 다른 리더의 면담 열람 범위는 조직 정책 확정 후 |

## 알려진 제약

- 면담 기록의 10건 제한은 화면 조회 기준이며, 서버에는 누적 저장된다.
- 실시간 도움 모드는 리더가 직원 발언을 직접 입력하는 방식이다. 자동 인식은 지원하지 않는다.
- AI 생성 결과는 매번 조금씩 달라진다. 단계 수와 시간 합계는 프롬프트로 고정했으나 문구는 고정되지 않는다.
